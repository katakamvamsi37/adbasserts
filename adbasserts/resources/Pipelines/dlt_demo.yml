resources:
  pipelines:
    dlt_demo:
      name: "New Pipeline 2026-04-22 15:20"
      channel: CURRENT
      development: false
      continuous: false
      photon: true
      catalog: ${resources.schemas.bronze_schema.catalog_name}
      schema: ${resources.schemas.bronze_schema.name}
      libraries:
        - notebook:
            path: ../Notebooks/dlt_demo_notebook.sql